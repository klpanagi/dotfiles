# Set Zsh as the default Tmux shell
set -g default-shell /bin/zsh

## Change Prefix ----{
unbind C-b
set -g prefix `
bind ` send-prefix
# -----------}

## address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

## increase scrollback buffer size
set -g history-limit 50000

## tmux messages are displayed for 4 seconds
set -g display-time 2000

## refresh 'status-left' and 'status-right' more often
set -g status-interval 1

setw -g status-keys vi
set -g status-keys vi

bind P paste-buffer
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection
bind-key -T copy-mode-vi r send-keys -X rectangle-toggle

# set -g set-clipboard on
# bind -T copy-mode-vi Enter send-keys -X copy-pipe-and-cancel "xsel -i --clipboard"

## focus events enabled for terminals that support them
set -g focus-events on

## super useful when using "grouped sessions" and multi-monitor setup
# setw -g aggressive-resize on
set-window-option -g xterm-keys on

# Start Numbering at 1
set -g base-index 1

# history
set -g history-limit 4096

# Faster command sequences
set -s escape-time 0

# Set aggressive resize when multiple clients attach to a session
setw -g aggressive-resize on

# allow terminal scrolling
set-option -g terminal-overrides 'xterm*:smcup@:rmcup@'

## Window configurations --------------{
set-window-option -g monitor-activity on
# Renumber windows when a window is closed
set -g renumber-windows on
set-window-option -g automatic-rename on
set-option -g allow-rename on
# -----------------------------}

## Keybindings -----------{
# Switch to Last active window
bind-key a last-window

# Reload configuration
bind r source-file ~/.tmux.conf \; display " Tmux Conf Reloaded!"

# quick pane cycling
# unbind ^A
bind Tab select-pane -t :.+

# Split
unbind %
unbind '"'
unbind o
# unbind n
# unbind p

bind \ split-window -h -c '#{pane_current_path}' # Split panes horizontal
bind - split-window -v -c '#{pane_current_path}' # Split panes vertically

#urxvt tab like window switching (-n: no prior escape seq)
# bind down new-window -c '#{pane_current_path}'
bind c new-window -c '#{pane_current_path}'
bind left prev
bind right next
# bind -n C-left swap-window -t -1
# bind -n C-right swap-window -t +1

# Resize
bind H resize-pane -L 10
bind J resize-pane -D 10
bind K resize-pane -U 10
bind L resize-pane -R 10

# Kill window
bind X kill-window
bind Q kill-session

bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Mouse support - set to on if you want to use the mouse ---{
set -g mouse on
bind -T root WheelUpPane   if-shell -F -t = "#{alternate_on}" "send-keys -M" "select-pane -t =; copy-mode -e; send-keys -M"
bind -T root WheelDownPane if-shell -F -t = "#{alternate_on}" "send-keys -M" "select-pane -t =; send-keys -M"
# ----------------------------}

# Color definitions -----------{
onedark_black="#282c34"
onedark_blue="#61afef"
onedark_yellow="#e5c07b"
onedark_red="#e06c75"
onedark_white="#aab2bf"
onedark_green="#98c379"
onedark_visual_grey="#3e4452"
onedark_comment_grey="#5c6370"

base16_green="#a1b56c"
base16_red="#ab4642"
base16_yellow="#f7ca88"
base16_blue="#7cafc2"
base16_magenta="#ba8baf"
base16_cyan="#86c1b9"
base16_c16="#dc9656"
base16_c17="#a16946"

## Nord palette: https://www.nordtheme.com/docs/colors-and-palettes
nord0="#2E3440"
nord1="#3B4252"
nord2="#434C5E"
nord3="#4C566A"
nord7="#8fbcbb"
nord8="#88c0d0"
nord9="#81a1c1"
nord10="#5e81ac"
nord11="#BF616A"
nord12="#D08770"
nord13="#EBCB8B"
nord14="#A3BE8C"
nord15="#B48EAD"

seperator="#[fg=$onedark_black,bg=$onedark_visual_grey,nobold,nounderscore,noitalics]"
# -------------------}

# Message Style ---------------{
set -g message-style fg=brightwhite,bg=blue
set -g message-command-style fg=blue,bg=black
# --------------------}

## Pane Configurations --------{
# border colours
set -g pane-border-style fg=default,bg=default
set -g pane-active-border-style fg="$base16_c16",bg=colour237
## --------------------}


# Visual notification of activity in other windows --{
setw -g monitor-activity on
set -g visual-activity on
# ---------------------------}

# The statusbar -----------{
set -g status on

set -g status-left-length 100
set -g status-right-length 100
# set -g status-style none
# set -g status-left-style blink,underline
# set -g status-right-style blink,underline

set -g message-style fg="$onedark_white",bg="$onedark_black"
set -g message-command-style fg="$onedark_white",bg="$onedark_black"

# Window status alignment
set -g status-justify centre

setw -g window-status-style fg="$onedark_black",bg="$onedark_black"
setw -g window-status-activity-style fg="$onedark_black",bg="$onedark_black"
setw -g window-status-separator " "

set -g status-bg "$onedark_black"
set -g status-fg "$onedark_white"

set -g @prefix_highlight_fg "$onedark_black"
set -g @prefix_highlight_bg "$base16_red"
set -g @prefix_highlight_copy_mode_attr "fg=$onedark_black,bg=$base16_red"
set -g @prefix_highlight_output_prefix " --> "

set -g window-status-format "#[fg=$base16_yellow,bg=$nord3,bold][#I]:#W #[fg=$onedark_black,bg=$onedark_black,nobold,nounderscore,noitalics]"
set -g window-status-current-format "#[fg=$nord3,bg=$base16_yellow,bold][#I]:#W "

set -g status-left "#[fg=$onedark_visual_grey,bg=$base16_green,bold] #S #[fg=$onedark_visual_grey,bg=$onedark_black,bold] #[fg=#EBCB8B,bg=$nord3,bold] #(whoami)@#H #[fg=$onedark_visual_grey,bg=$onedark_black,nobold,nounderscore,noitalics]"
set -g status-right "#[fg=$onedark_visual_grey,bg=$onedark_black,nobold,noitalics,nounderscore] #[fg=$onedark_white,bg=$onedark_black,bold] #[fg=$onedark_white,bg=$nord3,bold] [Mem unsued: #{free_mem}Gb] #[fg=$onedark_visual_grey,bg=$onedark_black,bold] #{prefix_highlight}"
# -------------------------}

# upgrade $TERM
# Default terminal is 256 colors
#set -g default-terminal "xterm-256color"
set -g default-terminal "screen-256color"  # Tested with tmux 2.5

# Add truecolor support
# tmux supports 24-bit color as of version 2.2:
#   https://github.com/tmux/tmux/commit/427b8204268af5548d09b830e101c59daa095df9
#   https://github.com/tmux/tmux/issues/622
set-option -ga terminal-overrides ",screen-256color:Tc"
# set-option -ga terminal-overrides ",xterm-256color:Tc"

set -g @shell_mode 'vi'

# Plugins ---------------------------{
set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
#set -g @plugin 'tmux-plugins/tmux-pain-control'
# set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
#set -g @plugin 'tmux-plugins/tmux-net-speed'
set -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'odedlaz/tmux-onedark-theme'
set -g @plugin 'odedlaz/tmux-status-variables'
# set -g @plugin 'tmux-plugins/tmux-urlview'
set -g @plugin 'tmux-plugins/tmux-open'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-fpp'
# ---------------------------------}

set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-save-shell-history 'on'
set -g @resurrect-strategy-vim 'session'
set -g @resurrect-strategy-nvim 'session'

# set -g @yank_selection 'primary'
set -g @yank_selection 'clipboard'
set -g @yank_selection_mouse 'clipboard'

set -g @fpp-key 'F'

# Automatic tpm installation
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"

# Initialize TMUX plugin manager
run-shell '~/.tmux/plugins/tpm/tpm'
